Terrasense update files

Replace the matching files in your repository with these files and keep the assets folder in the repository root.

The Google sign-in option has been removed. If your repository still contains .streamlit/secrets.toml.example, delete that obsolete sample file. Do not upload a real secrets.toml file.

The leaf screen now checks the selected crop against the model's actual class labels. For unsupported crops, it shows an explanatory message instead of forcing a disease match. It also filters supported predictions to the selected crop.
